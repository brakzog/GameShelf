name: gameshelf
description: A lightweight game launcher prototype.
publish_to: 'none'
version: 0.3.0+3

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
